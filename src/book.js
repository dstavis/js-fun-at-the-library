function createTitle(title) {
  var modifiedTitle = "The " + title;
  return modifiedTitle;
}

module.exports = {
  createTitle,
  // buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}
